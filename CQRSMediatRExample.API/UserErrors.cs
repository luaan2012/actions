namespace CQRSMediatRExample.API
{
    public static class UserErrors
    {
        //public static Error NotFound(Guid userId) => new(
        //    "user.NotFound", $"The user with the id = '{userId}' was not found");
    }
}
